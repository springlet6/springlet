 以 DO 为后缀：自动生成的 Entity，主要用于mybatis-plus 建立与表的映射关系。
 以 DAO  为后缀：自动生成的 DAO ，主要用于 自定义查询与xml建立映射关系。
 以 Crud 为后缀：自动生成的Service，主要用于 单表的增删查改以及批量操作。
 以 DTO 为后缀： 业务直接暴露接口 请求参数传输对象。
 以 Query 为后缀： 业务直接暴露接口 查询请求参数传输对象。
 以 VO 为后缀：业务直接暴露接口 web响应数据传输对象。
 以 Service 为后缀：跟业务相关的服务。